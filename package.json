{
  "scripts": {
    "install": "cmake-js compile",
    "preinstall": "clang-format -i --glob=src/*.c",
    "test": "./build/filecrud"
  },
  "devDependencies": {
    "clang-format": "^1.4.0",
    "cmake-js": "^6.1.0"
  }
}
