{
  "name": "filecrud",
  "version": "1.0.0",
  "description": "CRUD operations in a file",
  "author": "Helio Frota",
  "main": "index.js",
  "scripts": {
    "install": "cmake-js compile",
    "format": "clang-format -i --glob=*/**/*.{c}"
  },
  "license": "MIT",
  "devDependencies": {
    "cmake-js": "^6.1.0"
  }
}
